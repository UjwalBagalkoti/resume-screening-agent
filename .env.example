# Optional. If set, the agent uses Claude to phrase each candidate's
# reasoning as a natural sentence. If left unset, the agent still runs
# end-to-end and produces correct reasoning from a deterministic template.
ANTHROPIC_API_KEY=your-key-here
